int add(int left, int right) {
  return left + right;
}

int square(int value) {
  return value * value;
}

int average(int total, int count) {
  if (count == 0) {
    return 0;
  }
  return total / count;
}
